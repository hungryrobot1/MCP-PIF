# Minimal requirements for Python 3.13 compatibility
# This installs only the essential packages

# Core dependencies
setuptools>=69.0.0
wheel>=0.42.0

# Core web framework
fastapi>=0.104.0
uvicorn>=0.24.0

# Basic ML (we'll use a lighter alternative for now)
scikit-learn>=1.3.0
# For embeddings, we'll implement a simple solution initially

# API models and validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Async support
aiosqlite>=0.19.0
aiofiles>=23.0.0

# Utilities
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Simple vector storage (instead of ChromaDB for now)
faiss-cpu>=1.7.4
# or alternatively: annoy>=1.17.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0